<Window x:Class="GacsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GACS" Height="400" Width="584">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- First Group -->
        <GroupBox Header="First Group" Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
            <StackPanel Margin="8">
                <Label Content="First Option:" />
                <ComboBox ItemsSource="{Binding FirstOptions}" 
                         SelectedItem="{Binding SelectedFirst}" 
                         Margin="0,0,0,8" />

                <Label Content="Second Option:" />
                <ComboBox ItemsSource="{Binding SecondOptions}" 
                         SelectedItem="{Binding SelectedSecond}" 
                         Margin="0,0,0,8"/>
            </StackPanel>
        </GroupBox>

        <!-- Second Group -->
        <GroupBox Header="Second Group" Grid.Row="1" Grid.Column="0" Margin="8,0,0,8">
            <StackPanel Margin="8">
                <Label Content="First Option:" />
                <ComboBox ItemsSource="{Binding FirstOptions}" 
                         SelectedItem="{Binding SelectedFirst}" 
                         Margin="0,0,0,8" />

                <Label Content="Second Option:" />
                <ComboBox ItemsSource="{Binding SecondOptions}" 
                         SelectedItem="{Binding SelectedSecond}" 
                         Margin="0,0,0,8"/>
            </StackPanel>
        </GroupBox>

        <!-- Status and Button -->
        <TextBlock Text="{Binding Status}" 
                   Grid.Row="1" 
                   Grid.ColumnSpan="2" 
                   Margin="0,8"/>

        <Button Content="Do Work"
                Command="{Binding DoWorkCommand}"
                Width="110"
                HorizontalAlignment="Left"
                Grid.Row="2"
                Grid.ColumnSpan="2"
                Margin="0,8"/>
    </Grid>
</Window>
