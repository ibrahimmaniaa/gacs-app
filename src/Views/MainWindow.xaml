<Window x:Class="GacsApp.ViewModels.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GACS - Enhanced Green Assessment of CQDs Synthesis" Height="600" Width="1600"
        ResizeMode="NoResize"
        Icon="/Assets/appicon.ico">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <GroupBox Header="Resource Sustainability &amp; Environmental Impact" Grid.Row="0" Grid.Column="0" Margin="8,0,8,8">
            <StackPanel Margin="8">
                <Label Content="Precursor Origin &amp; Renewability:" />
                <ComboBox ItemsSource="{Binding PrecursorOriginItems}" 
                          SelectedItem="{Binding SelectedPrecursorOriginItem}" 
                          Margin="0,0,0,8" />

                <Label Content="Solvent Greenness:" />
                <ComboBox ItemsSource="{Binding SolventGreennessItems}" 
                          SelectedItem="{Binding SelectedSolventGreennessItem}" 
                          Margin="0,0,0,8"/>

                <Label Content="Energy Input:" />
                <ComboBox ItemsSource="{Binding EnergyInputItems}" 
                          SelectedItem="{Binding SelectedEnergyInputItem}" 
                          Margin="0,0,0,8"/>

                <Label Content="E-Factor &amp; Waste Generation:" />
                <ComboBox ItemsSource="{Binding EFactorWasteGenerationItems}" 
                          SelectedItem="{Binding SelectedEFactorWasteGenerationItem}" 
                          Margin="0,0,0,8"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Synthesis Efficiency &amp; Practicality" Grid.Row="1" Grid.Column="0" Margin="8,0,8,8">
            <StackPanel Margin="8">
                <Label Content="Synthesis Time:" />
                <ComboBox ItemsSource="{Binding SynthesisTimeItems}" 
                          SelectedItem="{Binding SelectedSynthesisTimeItem}" 
                          Margin="0,0,0,8" />

                <Label Content="Overall Simplicity &amp; Scalability:" />
                <ComboBox ItemsSource="{Binding SimplicityScalabilityItems}" 
                          SelectedItem="{Binding SelectedSimplicityScalabilityItem}" 
                          Margin="0,0,0,8"/>

                <Label Content="Purification Simplicity:" />
                <ComboBox ItemsSource="{Binding PurificationSimplicityItems}" 
                          SelectedItem="{Binding SelectedPurificationSimplicityItem}" 
                          Margin="0,0,0,8"/>

                <Label Content="Reaction Mass Efficiency (RME) or Yield:" />
                <ComboBox ItemsSource="{Binding ReactionMassEfficiencyItems}" 
                          SelectedItem="{Binding SelectedReactionMassEfficiencyItem}" 
                          Margin="0,0,0,8"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Product Quality &amp; Performance" Grid.Row="0" Grid.Column="1" Margin="8,0,8,8">
            <StackPanel Margin="8">
                <Label Content="Quantum Yield (QY):" />
                <ComboBox ItemsSource="{Binding QuantumYieldItems}" 
                          SelectedItem="{Binding SelectedQuantumYieldItem}" 
                          Margin="0,0,0,8" />

                <Label Content="Morphology &amp; Uniformity:" />
                <ComboBox ItemsSource="{Binding MorphologyUniformityItems}" 
                          SelectedItem="{Binding SelectedMorphologyUniformityItem}" 
                          Margin="0,0,0,8"/>

                <Label Content="Performance &amp; Applicability" />
                <ComboBox ItemsSource="{Binding PerformanceApplicabilityItems}" 
                          SelectedItem="{Binding SelectedPerformanceApplicabilityItem}" 
                          Margin="0,0,0,8"/>
            </StackPanel>
        </GroupBox>

        <TextBlock Text="{Binding Score, StringFormat=Score: {0}}"
                   FontSize="16"
                   FontWeight="Bold"
                   Grid.Row="1" 
                   Grid.Column="3" 
                   Margin="46,228,205,14"/>

        <Button Content="Calculate"
                Command="{Binding DoWorkCommand}"
                Width="188"
                HorizontalAlignment="Right"
                Grid.Row="1"
                Grid.Column="2"
                Margin="0,228,30,14"/>
    </Grid>
</Window>
